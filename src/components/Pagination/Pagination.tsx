import styles from './Pagination.module.scss';

export const Pagination = () => {
  return (
    <div className={styles.pagination_main_box}>
      <div className={styles.pagination_box}></div>
    </div>
  );
};
